/**
 * Global Configuration for Node Commander
 * Holds the target node base endpoint placeholder required by the specification.
 */
export const CONFIG = {
  TARGET_NODE: '{{.TargetNode}}',
  API_VERSION: '0.1.0',
  OAS_VERSION: '3.1.0',
  DEFAULT_THEME: 'light' as const
};
